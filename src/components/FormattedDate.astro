---
interface Props {
	date?: Date;
}

const { date = new Date() } = Astro.props;
---

<time datetime={date.toISOString()}>{
	date.toLocaleDateString('en-de', {
		year: 'numeric',
		month: 'short',
		day: 'numeric',
	})
}</time>
